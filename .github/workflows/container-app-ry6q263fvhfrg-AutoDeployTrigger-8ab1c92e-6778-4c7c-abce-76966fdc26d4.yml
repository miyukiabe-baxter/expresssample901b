name: Trigger auto deployment for container-app-ry6q263fvhfrg

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - '//**'
    - '.github/workflows/container-app-ry6q263fvhfrg-AutoDeployTrigger-8ab1c92e-6778-4c7c-abce-76966fdc26d4.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.CONTAINERAPPRY6Q263FVHFRG_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.CONTAINERAPPRY6Q263FVHFRG_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.CONTAINERAPPRY6Q263FVHFRG_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }} 
          registryUrl: 
          registryUsername: ${{ secrets.CONTAINERAPPRY6Q263FVHFRG_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.CONTAINERAPPRY6Q263FVHFRG_REGISTRY_PASSWORD }}
          containerAppName: container-app-ry6q263fvhfrg
          resourceGroup: miabetodo0707bf90
          imageToBuild: default/container-app-ry6q263fvhfrg:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


